{
  "name": "@loyalty/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no runtime tests (type-only package)\" && exit 0",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
